# Tasks Document

## Phase 1: Core Infrastructure & Validators

- [x] 1. Create IValidator interface and base validation models
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Validators/IValidator.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/ValidationReport.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/ValidationResult.cs` (extend existing)
  - Define `IValidator` interface with `ValidateAsync()` method signature
  - Create `ValidationReport`, `ValidationSummary`, `ValidationSeverity` models
  - Extend existing `VerificationResult` class with new fields (`Severity`, `SuggestedFix`, `DocumentationUrl`, `Context`)
  - Purpose: Establish core contracts for all validators and reports
  - _Leverage: existing `VerificationResult`, `MarshallingTestResult` from current infrastructure_
  - _Requirements: Req 1 (High-Risk Area Marshaling Validators)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# interface designer specializing in validation frameworks and SOLID principles | Task: Create IValidator interface and validation report models following Requirement 1, extending existing VerificationResult class from src/FluentPDF.Rendering/Interop/Verification/ with new diagnostic fields (Severity, SuggestedFix, DocumentationUrl, Context dictionary) | Restrictions: Do not modify existing MarshallingVerifier or SignatureAnalyzer classes, maintain backward compatibility with existing VerificationResult consumers, follow FluentPDF naming conventions (PascalCase) | _Leverage: Existing VerificationResult, MarshallingTestResult, SignatureDetails models from src/FluentPDF.Rendering/Interop/Verification/ | Success: IValidator interface compiles with clear method signatures, ValidationReport model includes summary statistics and results-by-area dictionary, ValidationResult extends existing class without breaking changes, all enums follow project patterns | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool to record implementation with detailed artifacts (interfaces created, model properties, file locations), then mark [x] as complete

- [x] 2. Implement Utf16MarshalingValidator
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Validators/Utf16MarshalingValidator.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/Utf16MarshalingValidatorTests.cs`
  - Implement `IValidator` interface with UTF-16LE validation logic
  - Add methods: `ValidateBookmarkMarshalingAsync`, `ValidateTextSearchMarshalingAsync`, `ValidateFormFieldMarshalingAsync`
  - Test cases: emojis (4-byte surrogate pairs), null characters, combining diacritics, CRLF vs. LF, empty strings, maximum length strings
  - Purpose: Validate UTF-16LE string marshaling for bookmarks, text search, and form fields
  - _Leverage: Existing bookmark title extraction logic from PdfiumInterop.cs:429-449, FPDFText_FindStart logic from PdfiumInterop.cs:699-713_
  - _Requirements: Req 1.1 (UTF-16 marshaling validation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# developer with expertise in text encoding and P/Invoke marshaling | Task: Implement Utf16MarshalingValidator class following Requirement 1.1, validating UTF-16LE string marshaling for bookmark extraction (two-phase allocation: get length, allocate buffer, fill buffer), text search (byte array with null terminator), and form fields, using existing FPDFBookmark_GetTitle and FPDFText_FindStart patterns from PdfiumInterop.cs | Restrictions: Must test with real PDFium calls using test PDF files, do not modify PdfiumInterop.cs, ensure thread-safe implementation with lock synchronization, file must be < 500 lines | _Leverage: PdfiumInterop.cs bookmark and text search logic (lines 429-449, 699-713), existing DataMarshallerTester pattern for runtime validation | Success: Validator implements IValidator interface, all three validation methods (bookmarks, text search, form fields) work correctly with test PDFs containing UTF-16 content, unit tests cover edge cases (emojis, null chars, combining diacritics, empty strings), validation report includes pass/fail status with detailed error messages | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ValidateBookmarkMarshalingAsync signature and purpose), test cases covered (6 edge cases), file location, then mark [x] as complete

- [x] 3. Implement BitmapMarshalingValidator
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Validators/BitmapMarshalingValidator.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/BitmapMarshalingValidatorTests.cs`
  - Implement `IValidator` interface with bitmap buffer validation logic
  - Add methods: `ValidateStrideCalculation`, `ValidateMarshalCopyAsync`, `ValidatePixelDataIntegrityAsync`
  - Test cases: 1x1 pixel, 8192x8192 pixel, 1920x1080 (non-power-of-2), width causing stride padding, zero/negative dimensions, checked arithmetic for stride × height
  - Purpose: Validate bitmap buffer marshaling, stride calculations, and overflow prevention
  - _Leverage: PdfRenderingService bitmap buffer copying logic from PdfRenderingService.cs:323-343, existing FPDFBitmap_Create/GetBuffer/GetStride P/Invoke declarations_
  - _Requirements: Req 1.2 (Bitmap marshaling validation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# developer specializing in unmanaged memory and bitmap processing | Task: Implement BitmapMarshalingValidator class following Requirement 1.2, validating bitmap buffer marshaling with stride calculations using checked arithmetic to detect overflows, Marshal.Copy operations with edge-case buffer sizes (1x1, 8192x8192, non-power-of-2 dimensions), and pixel data integrity after marshaling, reusing PdfRenderingService buffer copying pattern from PdfRenderingService.cs:323-343 | Restrictions: Must use checked arithmetic for stride × height calculations, test with actual PDFium bitmap creation and destruction, ensure proper buffer cleanup to prevent memory leaks, file must be < 500 lines | _Leverage: PdfRenderingService.cs bitmap buffer logic (lines 323-343), PdfiumInterop FPDFBitmap_* functions | Success: Validator implements IValidator interface, ValidateStrideCalculation detects integer overflow with checked arithmetic, ValidateMarshalCopyAsync tests edge-case buffer sizes without crashes, ValidatePixelDataIntegrityAsync verifies BGRA32 pixel format correctness, unit tests cover all 7 edge cases with proper assertions | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ValidateStrideCalculation, ValidateMarshalCopyAsync, ValidatePixelDataIntegrityAsync signatures and purposes), edge cases tested (7 scenarios), file location, then mark [x] as complete

- [x] 4. Implement BufferSafetyValidator
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Validators/BufferSafetyValidator.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/BufferSafetyValidatorTests.cs`
  - Implement `IValidator` interface with buffer overflow detection logic
  - Add methods: `AnalyzeMarshalCopyCallsAsync` (reflection-based static analysis), `SimulateBufferOverflowAsync` (runtime edge-case testing)
  - Test cases: zero-length buffer, 1-byte buffer (UTF-16 requires 2 bytes min), 2GB allocation boundary, stride overflow, two-phase allocation validation
  - Purpose: Detect buffer overflow risks in all `Marshal.Copy` operations
  - _Leverage: Reflection patterns from SignatureAnalyzer.cs for finding Marshal.Copy call sites, existing two-phase allocation pattern from bookmark extraction_
  - _Requirements: Req 4 (Buffer Overflow & Memory Safety Validator)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# security specialist with expertise in memory safety and static analysis | Task: Implement BufferSafetyValidator class following Requirement 4, using reflection to find all Marshal.Copy call sites in PdfiumInterop.cs (similar to SignatureAnalyzer pattern), analyze buffer size calculations for potential overflows, and simulate buffer overflow scenarios with edge-case sizes (zero-length, 1-byte, 2GB boundary, stride overflow) using runtime tests | Restrictions: Must use reflection for static analysis (do not execute Marshal.Copy during analysis), handle OutOfMemoryException gracefully for large allocations, ensure thread-safe implementation, file must be < 500 lines | _Leverage: SignatureAnalyzer.cs reflection patterns (lines 29-47), existing two-phase allocation logic from PdfiumInterop bookmark extraction | Success: AnalyzeMarshalCopyCallsAsync finds all Marshal.Copy call sites via reflection and reports buffer size calculation patterns, SimulateBufferOverflowAsync tests 5 edge-case scenarios without crashing, validation report includes specific call sites with potential overflow risks, unit tests verify reflection accuracy and edge-case handling | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (AnalyzeMarshalCopyCallsAsync, SimulateBufferOverflowAsync signatures and purposes), reflection targets (Marshal.Copy call sites), edge cases (5 scenarios), file location, then mark [x] as complete

- [x] 5. Create test PDF files and test data infrastructure
  - Files:
    - `tests/FluentPDF.Rendering.Tests/TestData/utf16_bookmarks.pdf`
    - `tests/FluentPDF.Rendering.Tests/TestData/searchable_text.pdf`
    - `tests/FluentPDF.Rendering.Tests/TestData/large_image.pdf`
    - `tests/FluentPDF.Rendering.Tests/TestData/comprehensive_test.pdf`
    - `tests/FluentPDF.Rendering.Tests/TestData/README.md` (documentation)
  - Create test PDFs with UTF-16 bookmarks (emojis, combining diacritics), searchable text, large images (8192x8192), annotations
  - Document test PDF characteristics and purpose in README.md
  - Purpose: Provide comprehensive test data for all validators
  - _Leverage: Existing test PDF patterns from tests/FluentPDF.Rendering.Tests/TestData/ (if any exist)_
  - _Requirements: Req 1 (all validators need test PDFs)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: QA engineer with PDF creation expertise | Task: Create test PDF files for marshaling validation following Requirement 1, including utf16_bookmarks.pdf with emojis and combining diacritics in bookmark titles, searchable_text.pdf with searchable UTF-16 text content, large_image.pdf with 8192x8192 pixel image for bitmap stress testing, and comprehensive_test.pdf combining all features, documenting each PDF's characteristics and purpose in README.md | Restrictions: Use standard PDF creation tools (Adobe Acrobat, LibreOffice, pdftk, or Python PyPDF2), ensure PDFs are not corrupted and can be loaded by PDFium, keep total test data size < 50MB, do not include copyrighted content | Success: All 4 test PDFs created and loadable by PDFium, utf16_bookmarks.pdf contains at least 5 bookmarks with edge-case characters (emoji, combining diacritics, null char variants), searchable_text.pdf has searchable text layer, large_image.pdf has 8192x8192 BGRA32 image, comprehensive_test.pdf combines all features, README.md documents each PDF with file size and purpose | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: files created (4 PDFs + README), file sizes, test characteristics documented, then mark [x] as complete

## Phase 2: Threading, Annotations & Signature Validation

- [x] 6. Implement ThreadingModelValidator
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Validators/ThreadingModelValidator.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/ThreadingModelValidatorTests.cs`
  - Implement `IValidator` interface with threading model validation logic
  - Add methods: `ValidateTaskYieldWorkaroundAsync`, `ValidateConcurrentAccessAsync` (isolated process execution)
  - Test cases: sequential PDFium calls with Task.Yield() (should succeed), concurrent calls with Task.Run() (should detect AccessViolation in isolated process), high concurrency stress test (100 simultaneous operations)
  - Purpose: Validate Task.Yield() threading workaround and detect AccessViolation scenarios
  - _Leverage: PdfiumServiceBase.ExecutePdfiumOperationAsync pattern from PdfiumServiceBase.cs, Process class for isolated execution_
  - _Requirements: Req 1.4 (Threading model validation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# concurrency specialist with expertise in threading and process isolation | Task: Implement ThreadingModelValidator class following Requirement 1.4, validating that Task.Yield() prevents AccessViolation crashes (using PdfiumServiceBase pattern from PdfiumServiceBase.cs), testing concurrent PDFium operations in isolated process to detect crashes without affecting test runner (exit code 0xC0000005 = AccessViolation), and stress testing with 100 simultaneous operations using Task.Yield() | Restrictions: Must run AccessViolation detection in isolated process using Process.Start to prevent test runner crashes, validate Task.Yield() behavior matches PdfiumServiceBase.ExecutePdfiumOperationAsync pattern, ensure thread-safe test execution, file must be < 500 lines | _Leverage: PdfiumServiceBase.cs ExecutePdfiumOperationAsync logic, Process class for isolated execution | Success: ValidateTaskYieldWorkaroundAsync confirms Task.Yield() prevents crashes, ValidateConcurrentAccessAsync detects AccessViolation (exit code 0xC0000005) in isolated process without crashing test runner, stress test with 100 concurrent operations completes successfully with Task.Yield(), unit tests verify all 3 scenarios with proper assertions | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ValidateTaskYieldWorkaroundAsync, ValidateConcurrentAccessAsync signatures and purposes), isolated process execution pattern, test scenarios (3 cases), file location, then mark [x] as complete

- [x] 7. Implement AnnotationMarshalingValidator
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Validators/AnnotationMarshalingValidator.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/AnnotationMarshalingValidatorTests.cs`
    - `tests/FluentPDF.Rendering.Tests/TestData/annotations.pdf` (test PDF with annotations)
  - Implement `IValidator` interface with annotation geometry validation logic
  - Add methods: `ValidateQuadPointsMarshalingAsync`, `ValidateRectMarshalingAsync`
  - Test cases: complete quad points array (8 floats), partial array (< 8 elements), NaN and infinity values, very large coordinates, negative coordinates, inverted rect (left > right)
  - Purpose: Validate annotation geometry marshaling (FS_QUADPOINTSF, FS_RECTF structs)
  - _Leverage: Existing FS_QUADPOINTSF, FS_RECTF struct definitions from PdfiumInterop.cs, FPDFAnnot_SetAttachmentPoints logic from PdfiumInterop.cs:1110-1120_
  - _Requirements: Req 1.3 (Annotation marshaling validation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# developer with expertise in struct marshaling and geometry processing | Task: Implement AnnotationMarshalingValidator class following Requirement 1.3, validating FS_QUADPOINTSF struct marshaling with all 8 float coordinates (handling partial arrays, NaN, infinity, large values, negative values) and FS_RECTF struct marshaling (handling inverted coordinates), using existing struct definitions and FPDFAnnot_SetAttachmentPoints logic from PdfiumInterop.cs:1110-1120, testing with annotations.pdf | Restrictions: Must test with real annotation creation/modification using PDFium, handle floating-point edge cases (NaN, infinity) safely, ensure struct layout matches PDFium expectations (LayoutKind.Sequential), file must be < 500 lines | _Leverage: PdfiumInterop.cs FS_QUADPOINTSF and FS_RECTF struct definitions, FPDFAnnot_* function patterns | Success: ValidateQuadPointsMarshalingAsync tests all 6 edge cases (complete, partial, NaN, infinity, large, negative), ValidateRectMarshalingAsync detects inverted coordinates and handles correctly, validator implements IValidator interface, unit tests cover all annotation geometry scenarios with proper assertions, annotations.pdf test file created with sample annotations | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ValidateQuadPointsMarshalingAsync, ValidateRectMarshalingAsync signatures and purposes), struct types validated (FS_QUADPOINTSF, FS_RECTF), edge cases (6+ scenarios), file location, annotations.pdf characteristics, then mark [x] as complete

- [x] 8. Extend SignatureAnalyzer with external specification support
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/SignatureAnalyzer.cs` (modify existing)
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/SignatureAnalyzerTests.cs` (extend existing)
    - `tests/FluentPDF.Rendering.Tests/TestData/pdfium-spec.json` (PDFium API specification)
  - Add method: `ValidateAgainstPDFiumSpec(string specificationJsonPath)` to existing SignatureAnalyzer class
  - Create JSON specification file with PDFium API signatures (FPDF_LoadDocument, FPDFBookmark_GetTitle, FPDF_RenderPageBitmap, etc.)
  - Purpose: Validate P/Invoke signatures against external PDFium specification
  - _Leverage: Existing SignatureAnalyzer reflection logic (AnalyzeSignature, ValidateSignature methods), System.Text.Json for spec parsing_
  - _Requirements: Req 2 (API Signature Compliance Scanner)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# reflection specialist with API design expertise | Task: Extend existing SignatureAnalyzer class (src/FluentPDF.Rendering/Interop/Verification/SignatureAnalyzer.cs) following Requirement 2, adding ValidateAgainstPDFiumSpec method that loads external JSON specification file and validates all P/Invoke signatures against it (parameter count, type mismatch, calling convention), create pdfium-spec.json with at least 20 critical PDFium functions (FPDF_LoadDocument, FPDFBookmark_GetTitle, FPDF_RenderPageBitmap, FPDFText_FindStart, etc.) including return types, parameters, calling conventions, and marshaling notes | Restrictions: Do not modify existing public methods in SignatureAnalyzer, maintain backward compatibility, ensure JSON spec schema is well-documented, use System.Text.Json for parsing (not Newtonsoft.Json), method must be < 50 lines | _Leverage: Existing SignatureAnalyzer.ValidateSignature and AnalyzeSignature methods (lines 89-174), System.Text.Json for spec deserialization | Success: ValidateAgainstPDFiumSpec method added to SignatureAnalyzer, JSON spec file created with 20+ functions documented (return type, parameters, calling convention, marshaling notes), validation correctly detects signature mismatches (parameter count, type, calling convention), unit tests verify spec loading and validation logic, backward compatibility maintained | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ValidateAgainstPDFiumSpec signature and purpose, added to existing class), spec file (pdfium-spec.json with 20+ functions), file location, then mark [x] as complete

## Phase 3: Performance Profiling & Workaround Tests

- [x] 9. Create IMarshalingProfiler interface and performance models
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Profilers/IMarshalingProfiler.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/ProfilingReport.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/ProfilingResult.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/PerformanceMetrics.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/MemoryMetrics.cs`
  - Define `IMarshalingProfiler` interface with `ProfileFunctionAsync`, `ProfileAllFunctionsAsync`, `CompareAgainstBaseline` methods
  - Create `ProfilingReport`, `ProfilingResult`, `PerformanceMetrics`, `MemoryMetrics` models
  - Purpose: Establish contracts for performance profiling and regression detection
  - _Leverage: BenchmarkDotNet patterns from existing performance tests (if any), System.Diagnostics.Stopwatch for timing_
  - _Requirements: Req 5 (Marshaling Performance Profiler)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# performance engineer with expertise in profiling and metrics | Task: Create IMarshalingProfiler interface and performance report models following Requirement 5, defining ProfileFunctionAsync (profile single function across N iterations), ProfileAllFunctionsAsync (profile all P/Invoke functions), CompareAgainstBaseline (detect regressions > 20% slowdown), and models for ProfilingReport (with ResultsByFunction dictionary), ProfilingResult, PerformanceMetrics (min/max/median/P95/P99 latencies, std dev), MemoryMetrics (managed/unmanaged bytes, GC counts) | Restrictions: Follow BenchmarkDotNet metric patterns if available, use System.Diagnostics.Stopwatch for high-resolution timing, ensure models are JSON-serializable for baseline storage, all interfaces and models must follow project naming conventions | Success: IMarshalingProfiler interface compiles with clear method signatures, ProfilingReport model includes timestamp and summary statistics, PerformanceMetrics has all latency percentiles (min/max/median/P95/P99), MemoryMetrics tracks managed and unmanaged allocations plus GC counts, all models are JSON-serializable | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: interfaces (IMarshalingProfiler methods), models (ProfilingReport, ProfilingResult, PerformanceMetrics, MemoryMetrics properties), file locations, then mark [x] as complete

- [x] 10. Implement MarshalingPerformanceProfiler
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Profilers/MarshalingPerformanceProfiler.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/MarshalingPerformanceProfilerTests.cs`
    - `tests/FluentPDF.Rendering.Tests/TestData/profiling-baseline.json` (baseline performance data)
  - Implement `IMarshalingProfiler` interface with performance profiling logic
  - Add profiling for: execution time (Stopwatch), memory allocations (GC.GetTotalMemory), GC collections (GC.CollectionCount)
  - Calculate percentiles: min, max, median, P95, P99, standard deviation
  - Purpose: Measure execution time and memory overhead for all marshaling operations
  - _Leverage: System.Diagnostics.Stopwatch for timing, BenchmarkDotNet patterns for percentile calculations, System.Text.Json for baseline comparison_
  - _Requirements: Req 5 (Performance profiling), Req 5.4 (Baseline comparison)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# performance engineer specializing in profiling and statistics | Task: Implement MarshalingPerformanceProfiler class following Requirement 5, profiling P/Invoke function execution time using Stopwatch.GetTimestamp() for high-resolution timing across 1000 iterations, calculating percentiles (min/max/median/P95/P99) and standard deviation, measuring memory allocations via GC.GetTotalMemory and GC collections, comparing against baseline JSON file to detect > 20% regressions with severity warnings | Restrictions: Must use Stopwatch.GetTimestamp() for sub-millisecond precision, calculate percentiles using correct statistical methods (not approximations), measure memory before/after function execution to isolate allocations, ensure profiling overhead is < 5% of measured time, file must be < 500 lines | _Leverage: BenchmarkDotNet percentile calculation patterns, System.Text.Json for baseline serialization/comparison | Success: ProfileFunctionAsync profiles single function with accurate latency percentiles, ProfileAllFunctionsAsync iterates all P/Invoke functions in PdfiumInterop, CompareAgainstBaseline detects regressions > 20% and emits warnings, MemoryMetrics captures managed/unmanaged allocations and GC counts accurately, unit tests verify profiling accuracy and baseline comparison logic, profiling-baseline.json created with sample data for 10+ functions | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ProfileFunctionAsync, ProfileAllFunctionsAsync, CompareAgainstBaseline signatures and purposes), profiling techniques (Stopwatch, GC metrics), baseline file (profiling-baseline.json with 10+ functions), file location, then mark [x] as complete

- [x] 11. Create IWorkaroundTest interface and workaround models
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Regression/IWorkaroundTest.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/WorkaroundTestResult.cs`
  - Define `IWorkaroundTest` interface with `WorkaroundName`, `DocumentationReference`, `TestWorkaroundAsync` properties/methods
  - Create `WorkaroundTestResult` model with `WorkaroundStatus` enum (StillNeeded, CanBeRemoved, Broken)
  - Purpose: Establish contracts for testing documented workarounds
  - _Leverage: Existing workaround patterns from PdfiumInterop.cs:177-183 (float dimension), PdfiumServiceBase.cs (Task.Yield), ThumbnailsViewModel.cs:155 (SoftwareBitmapSource)_
  - _Requirements: Req 3 (Known Workaround Regression Detector)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# interface designer with expertise in test automation | Task: Create IWorkaroundTest interface and workaround result models following Requirement 3, defining WorkaroundName property, DocumentationReference property (file path and line number of workaround), TestWorkaroundAsync method (returns WorkaroundTestResult), and WorkaroundTestResult model with status enum (StillNeeded if workaround required, CanBeRemoved if PDFium bug fixed, Broken if workaround no longer works), details string, and recommended action | Restrictions: Follow project interface naming conventions, ensure WorkaroundStatus enum covers all scenarios, DocumentationReference should be file path with line numbers (e.g., "PdfiumInterop.cs:177-183"), all models must be JSON-serializable | Success: IWorkaroundTest interface compiles with clear contract, WorkaroundTestResult model includes status enum with 3 states, DocumentationReference format documented in XML comments, all types follow project conventions and are JSON-serializable | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: interfaces (IWorkaroundTest properties and methods), models (WorkaroundTestResult, WorkaroundStatus enum), file locations, then mark [x] as complete

- [x] 12. Implement workaround regression tests (FloatDimension, Threading, SoftwareBitmap)
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Regression/FloatDimensionWorkaroundTest.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Regression/ThreadingWorkaroundTest.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Regression/SoftwareBitmapWorkaroundTest.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/WorkaroundRegressionTests.cs`
  - Implement `IWorkaroundTest` for all 3 documented workarounds
  - FloatDimensionWorkaroundTest: Compare integer API (FPDF_GetPageWidth) vs. float API (FPDF_GetPageWidthF) results
  - ThreadingWorkaroundTest: Verify Task.Yield() prevents AccessViolation
  - SoftwareBitmapWorkaroundTest: Validate SoftwareBitmapSource workaround from ThumbnailsViewModel.cs:155
  - Purpose: Automated tests for all documented workarounds to detect breakage on PDFium updates
  - _Leverage: Existing workaround logic from PdfiumInterop.cs:177-183, PdfiumServiceBase.cs, ThumbnailsViewModel.cs:155_
  - _Requirements: Req 3 (Workaround regression tests), Req 3.2, 3.3, 3.4 (specific workaround tests)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# testing specialist with expertise in regression testing and P/Invoke | Task: Implement 3 workaround tests following Requirement 3, creating FloatDimensionWorkaroundTest (compare integer FPDF_GetPageWidth vs. float FPDF_GetPageWidthF, detect if float API now returns valid values indicating bug fix), ThreadingWorkaroundTest (verify Task.Yield() prevents AccessViolation using PdfiumServiceBase pattern), SoftwareBitmapWorkaroundTest (validate SoftwareBitmapSource workaround from ThumbnailsViewModel.cs:155 still works), each returning WorkaroundTestResult with status (StillNeeded/CanBeRemoved/Broken) and recommended action | Restrictions: Must test with real PDFium calls, compare workaround behavior against expected behavior, detect if PDFium update fixes underlying bugs, ensure tests are deterministic and don't depend on specific PDFium versions, each file must be < 500 lines | _Leverage: PdfiumInterop.cs:177-183 float dimension logic, PdfiumServiceBase.cs Task.Yield pattern, ThumbnailsViewModel.cs:155 SoftwareBitmapSource workaround | Success: All 3 workaround tests implement IWorkaroundTest interface, FloatDimensionWorkaroundTest detects if float API is fixed, ThreadingWorkaroundTest confirms Task.Yield() necessity, SoftwareBitmapWorkaroundTest validates workaround still works, unit tests verify all workarounds with proper status detection (StillNeeded/CanBeRemoved/Broken) | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: classes (3 workaround tests with TestWorkaroundAsync methods), workarounds tested (float dimension, threading, SoftwareBitmap), file locations, detection logic, then mark [x] as complete

## Phase 4: Reporting & CLI Integration

- [x] 13. Create report exporters (JSON, JUnit XML, HTML)
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/IReportExporter.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/JsonReportExporter.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/JUnitXmlExporter.cs`
    - `src/FluentPDF.Rendering/Interop/Verification/Reports/HtmlReportGenerator.cs`
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/ReportExporterTests.cs`
  - Define `IReportExporter<TReport>` interface with `ExportAsync(TReport report, string outputPath)` method
  - Implement JSON exporter using System.Text.Json with indentation
  - Implement JUnit XML exporter with GitHub Actions/Azure DevOps compatibility
  - Implement HTML exporter with color-coded severity levels and embedded CSS
  - Purpose: Generate reports in multiple formats for different consumers
  - _Leverage: Existing CoverageReporter patterns from CoverageReporter.cs, System.Text.Json, System.Xml.Linq_
  - _Requirements: Req 6.2, 6.4 (CI/CD integration with JUnit XML), Req 7 (Documentation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# developer with expertise in serialization and report generation | Task: Create IReportExporter interface and 3 concrete exporters following Requirements 6.2 and 7, implementing JsonReportExporter (using System.Text.Json with WriteIndented for readability), JUnitXmlExporter (generating XML compatible with GitHub Actions test reporting format), HtmlReportGenerator (creating styled HTML with color-coded severity: green for pass, yellow for warning, red for fail, embedded CSS for standalone files), all extending CoverageReporter patterns from existing infrastructure | Restrictions: Use System.Text.Json for JSON (not Newtonsoft.Json), JUnit XML must validate against JUnit schema used by GitHub Actions, HTML must be self-contained with embedded CSS (no external stylesheets), ensure thread-safe file writing, each exporter file must be < 500 lines | _Leverage: CoverageReporter.cs reporting patterns, System.Text.Json for JSON, System.Xml.Linq for XML generation | Success: IReportExporter interface defined with ExportAsync method, JsonReportExporter generates valid indented JSON, JUnitXmlExporter generates valid JUnit XML parseable by GitHub Actions, HtmlReportGenerator creates color-coded HTML with embedded CSS, unit tests verify all 3 exporters with sample ValidationReport data, output files are valid and readable | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: interfaces (IReportExporter), classes (JsonReportExporter, JUnitXmlExporter, HtmlReportGenerator with ExportAsync methods), output formats (JSON, JUnit XML, HTML), file locations, then mark [x] as complete

- [-] 14. Extend MarshallingVerifier orchestrator with new validators
  - Files:
    - `src/FluentPDF.Rendering/Interop/Verification/MarshallingVerifier.cs` (modify existing)
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/MarshallingVerifierIntegrationTests.cs` (new integration tests)
  - Add method: `ValidateHighRiskAreasAsync()` to orchestrate all new validators (UTF-16, bitmap, annotation, threading, buffer safety)
  - Add method: `ProfilePerformanceAsync()` to run performance profiler
  - Add method: `TestWorkaroundsAsync()` to run all workaround tests
  - Extend `VerifyAndReportAsync()` to include new validators, profiling, and workaround tests in comprehensive report
  - Purpose: Orchestrate all validators and generate unified reports
  - _Leverage: Existing MarshallingVerifier orchestration pattern (VerifyAllSignaturesAsync, TestMarshallingAsync, VerifyAndReportAsync methods)_
  - _Requirements: Req 6 (CI/CD integration), Req 6.1 (--validate-all command)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# architect with expertise in orchestration and parallel execution | Task: Extend existing MarshallingVerifier class (src/FluentPDF.Rendering/Interop/Verification/MarshallingVerifier.cs) following Requirement 6, adding ValidateHighRiskAreasAsync method (runs all 5 validators in parallel: UTF-16, bitmap, annotation, threading, buffer safety), ProfilePerformanceAsync method (runs MarshalingPerformanceProfiler), TestWorkaroundsAsync method (runs all 3 workaround tests), and extending VerifyAndReportAsync to include all new validators, profiling, and workaround tests in unified ValidationReport | Restrictions: Do not modify existing public method signatures, maintain backward compatibility, run validators in parallel using Task.WhenAll for performance, ensure proper error handling and cancellation token propagation, file must remain < 500 lines (extract helpers if needed) | _Leverage: Existing MarshallingVerifier methods (VerifyAllSignaturesAsync lines 42-83, TestMarshallingAsync lines 101-133), Task.WhenAll for parallel execution | Success: ValidateHighRiskAreasAsync added and runs all 5 validators in parallel, ProfilePerformanceAsync added and returns ProfilingReport, TestWorkaroundsAsync added and returns workaround results, VerifyAndReportAsync extended to include all new data in ValidationReport, integration tests verify orchestration with real PDFium calls, backward compatibility maintained (existing consumers not broken) | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (ValidateHighRiskAreasAsync, ProfilePerformanceAsync, TestWorkaroundsAsync signatures and purposes, added to existing class), orchestration pattern (Task.WhenAll for parallelism), file location, then mark [x] as complete

- [ ] 15. Add CLI command flags to CommandLineOptions
  - Files:
    - `src/FluentPDF.App/CommandLineOptions.cs` (modify existing)
    - `src/FluentPDF.App/CommandLineParser.cs` (if exists, otherwise modify App.xaml.cs)
  - Add CLI flags: `--validate-utf16-marshalling`, `--validate-bitmap-marshalling`, `--validate-annotation-marshalling`, `--validate-threading-model`, `--validate-buffer-safety`, `--validate-all`, `--profile-marshalling`, `--test-workarounds`, `--compare-baseline <path>`, `--junit-output <path>`, `--json-output <path>`, `--html-output <path>`
  - Add properties to `CommandLineOptions` class for each flag
  - Purpose: Enable CLI-centric validation workflow
  - _Leverage: Existing CLI flag patterns from CommandLineOptions.cs (--test-render, --diagnostics, --verify-marshalling, --marshalling-report)_
  - _Requirements: Req 1, 5, 6 (CLI commands for validation, profiling, reporting)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# CLI developer with expertise in command-line argument parsing | Task: Extend existing CommandLineOptions class (src/FluentPDF.App/CommandLineOptions.cs) following Requirements 1, 5, and 6, adding 12 new CLI flags (--validate-utf16-marshalling, --validate-bitmap-marshalling, --validate-annotation-marshalling, --validate-threading-model, --validate-buffer-safety, --validate-all, --profile-marshalling, --test-workarounds, --compare-baseline <path>, --junit-output <path>, --json-output <path>, --html-output <path>) with corresponding properties and XML doc comments explaining each flag's purpose | Restrictions: Follow existing flag naming patterns (kebab-case with double-dash prefix), add XML doc comments for each property explaining usage and exit codes, ensure boolean flags default to false and path flags default to null, maintain alphabetical property ordering, do not modify existing flags | _Leverage: Existing CommandLineOptions properties (TestRender, Diagnostics, VerifyMarshalling, MarshallingReport, OutputPath) from CommandLineOptions.cs lines 46-97 | Success: All 12 CLI flags added as properties with correct types (bool for toggles, string for paths), XML doc comments explain each flag's purpose and behavior, properties follow existing naming and documentation patterns, alphabetically ordered, file compiles without errors | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: properties added (12 CLI flags with names and types), file location (CommandLineOptions.cs), documentation pattern, then mark [x] as complete

- [ ] 16. Implement CLI command handlers for new validation flags
  - Files:
    - `src/FluentPDF.App/App.xaml.cs` (modify existing, add handler methods)
    - `src/FluentPDF.App/Diagnostics/MarshalingValidationService.cs` (new service class for validation orchestration from CLI)
  - Add handler methods: `HandleValidateUtf16Command()`, `HandleValidateBitmapCommand()`, `HandleValidateAllCommand()`, `HandleProfileMarshallingCommand()`, `HandleTestWorkaroundsCommand()`
  - Create `MarshalingValidationService` to encapsulate validation orchestration and report export logic
  - Purpose: Execute validation commands from CLI and generate reports
  - _Leverage: Existing CLI handler patterns from App.xaml.cs (TestRenderCommand, DiagnosticsCommand handlers), MarshallingVerifier for orchestration, IReportExporter for output_
  - _Requirements: Req 6 (CI/CD integration), Req 6.3 (exit codes)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: C# application developer with expertise in CLI application architecture | Task: Implement CLI command handlers in App.xaml.cs following Requirement 6, creating handler methods (HandleValidateUtf16Command, HandleValidateBitmapCommand, HandleValidateAllCommand, HandleProfileMarshallingCommand, HandleTestWorkaroundsCommand) that instantiate MarshallingVerifier, run appropriate validators, export reports using IReportExporter based on output flags (--junit-output, --json-output, --html-output), and return correct exit codes (0=success, 1=validation failed, 2=critical error), encapsulating logic in new MarshalingValidationService class for separation of concerns | Restrictions: Follow existing handler patterns from App.xaml.cs, ensure exit codes are consistent (0=pass, 1=fail, 2=error), call Application.Exit(exitCode) after command completes, log all errors to Serilog with correlation IDs, MarshalingValidationService must be in Diagnostics namespace, each handler method must be < 50 lines | _Leverage: Existing CLI handlers (TestRenderCommand, DiagnosticsCommand patterns from App.xaml.cs), MarshallingVerifier for validation orchestration, IReportExporter for report generation | Success: All 5 handler methods added to App.xaml.cs and call MarshalingValidationService, MarshalingValidationService class created with validation orchestration logic, reports exported to correct formats based on output flags, exit codes set correctly (0/1/2), console output shows validation summary, handlers follow existing CLI patterns | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: functions (5 handler methods in App.xaml.cs), classes (MarshalingValidationService with methods), exit codes (0/1/2), file locations, then mark [x] as complete

## Phase 5: Testing & Documentation

- [ ] 17. Create end-to-end CLI integration tests
  - Files:
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/CliIntegrationTests.cs`
  - Write tests for all CLI commands using Process.Start to invoke FluentPDF.App.exe
  - Test scenarios: `--validate-all`, `--profile-marshalling --compare-baseline`, `--test-workarounds`, `--validate-all --junit-output`, exit code validation
  - Purpose: Verify CLI commands work end-to-end as users/CI would invoke them
  - _Leverage: Process class for CLI invocation, existing test PDF files from TestData/, System.IO for output file validation_
  - _Requirements: All requirements (end-to-end validation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: QA automation engineer with expertise in end-to-end testing | Task: Create comprehensive CLI integration tests following all requirements, using Process.Start to invoke FluentPDF.App.exe with various flag combinations (--validate-all, --profile-marshalling --compare-baseline baseline.json, --test-workarounds, --validate-all --junit-output results.xml --json-output report.json), verifying exit codes (0=pass, 1=fail, 2=error), output file generation (JUnit XML, JSON, HTML), and console output format, testing all 4 user scenarios from design document | Restrictions: Must run FluentPDF.App.exe as actual process (not in-process), validate exit codes precisely, verify output file formats (JUnit XML parseable, JSON valid, HTML renderable), clean up test output files after tests, tests must be deterministic and not flaky | _Leverage: Process class for CLI execution, test PDFs from tests/FluentPDF.Rendering.Tests/TestData/, XDocument for XML validation, JsonDocument for JSON validation | Success: All 4 end-to-end scenarios tested (developer quick validation, CI full suite, performance profiling with baseline, workaround regression), exit codes verified for all scenarios, JUnit XML validated as parseable by GitHub Actions, JSON and HTML output validated, tests clean up temp files, tests are reliable and deterministic | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: test scenarios (4 E2E tests), CLI commands tested (all flags), exit code verification, output format validation, file location, then mark [x] as complete

- [ ] 18. Generate marshaling documentation and knowledge base
  - Files:
    - `docs/marshaling/README.md` (overview and quick start)
    - `docs/marshaling/safe-patterns.md` (safe marshaling patterns)
    - `docs/marshaling/pitfalls.md` (known pitfalls and workarounds)
    - `docs/marshaling/utf16-encoding.md` (UTF-16LE encoding rules)
    - `docs/marshaling/buffer-safety.md` (buffer safety guidelines)
    - `docs/marshaling/threading-model.md` (threading model constraints)
    - `docs/marshaling/decision-tree.md` (flowchart: how to marshal this data type)
  - Create comprehensive documentation covering all high-risk areas with code examples
  - Include "incorrect vs. correct" side-by-side examples for each pitfall
  - Add decision tree flowchart for choosing correct marshaling approach
  - Purpose: Knowledge base for developers writing new P/Invoke code
  - _Leverage: Existing workarounds and patterns from PdfiumInterop.cs, validation logic from validators_
  - _Requirements: Req 7 (Marshaling Knowledge Base & Documentation)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: Technical writer with C# and P/Invoke expertise | Task: Create comprehensive marshaling documentation following Requirement 7, writing 7 markdown files covering overview (README.md with quick start guide), safe patterns (UTF-16 two-phase allocation, stride calculation with checked arithmetic, Task.Yield threading), pitfalls (null terminator missing, buffer overflow, AccessViolation), UTF-16LE encoding rules (BOM, byte order, null terminators), buffer safety guidelines (Marshal.Copy best practices, overflow prevention), threading model constraints (Task.Yield vs Task.Run), and decision tree flowchart (text → UTF-16 path, images → bitmap buffer path, structs → LayoutKind.Sequential path, callbacks → delegate marshaling path) with side-by-side incorrect/correct code examples for each pitfall | Restrictions: Use markdown format with code fences for C# examples, include links between documents for cross-referencing, decision tree must be Mermaid flowchart or ASCII art diagram, examples must be compilable C# code snippets, keep each document < 500 lines | _Leverage: Existing workarounds from PdfiumInterop.cs (float dimension, Task.Yield), validation logic from Utf16MarshalingValidator, BitmapMarshalingValidator, ThreadingModelValidator | Success: All 7 markdown files created with comprehensive content, safe patterns documented with correct code examples, pitfalls documented with incorrect vs. correct side-by-side examples, decision tree flowchart created (Mermaid or ASCII art), UTF-16LE encoding rules explained clearly, buffer safety guidelines include Marshal.Copy best practices, threading model constraints documented with Task.Yield rationale | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: documentation files (7 markdown files with topics), code examples (incorrect vs. correct patterns), decision tree format (Mermaid or ASCII), file locations in docs/marshaling/, then mark [x] as complete

- [ ] 19. Create CI/CD GitHub Actions workflow for marshaling validation
  - Files:
    - `.github/workflows/marshaling-validation.yml`
  - Create GitHub Actions workflow that runs on pull requests
  - Workflow steps: checkout, setup .NET, build, run `--validate-all --junit-output --json-output`, publish test results, upload reports
  - Configure failure conditions: exit code 1 fails build, critical validations block merge
  - Purpose: Integrate marshaling validation into CI/CD pipeline
  - _Leverage: Existing GitHub Actions patterns from .github/workflows/ (if any), EnricoMi/publish-unit-test-result-action for JUnit XML parsing_
  - _Requirements: Req 6 (CI/CD Integration), Req 6.4 (JUnit XML output), Req 6.5 (Critical failures block PRs)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: DevOps engineer with GitHub Actions expertise | Task: Create GitHub Actions workflow file (.github/workflows/marshaling-validation.yml) following Requirement 6, configuring workflow to trigger on pull_request events, running on windows-latest runner, steps: checkout repository, setup .NET 8, build FluentPDF.App in Release mode, run FluentPDF.App.exe with --validate-all --junit-output validation-results.xml --json-output validation-report.json, publish test results using EnricoMi/publish-unit-test-result-action, upload validation-report.json as artifact, fail build if exit code is 1 (validation failed) or 2 (critical error) | Restrictions: Use windows-latest runner (required for WinUI 3 dependencies), ensure vcpkg libraries are available for PDFium, fail build on non-zero exit codes, always publish test results and artifacts even on failure (if: always()), follow GitHub Actions YAML best practices | _Leverage: Existing GitHub Actions patterns from project (if available), EnricoMi/publish-unit-test-result-action for JUnit XML parsing, actions/upload-artifact for report uploads | Success: Workflow file created and triggers on pull_request, runs on windows-latest with .NET 8, executes --validate-all command successfully, publishes JUnit XML test results to PR (visible in Checks tab), uploads JSON report as artifact, fails build on validation failures (exit code 1 or 2), workflow is well-documented with comments | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: workflow file (.github/workflows/marshaling-validation.yml), workflow steps (checkout, build, validate, publish, upload), trigger conditions (pull_request), file location, then mark [x] as complete

- [ ] 20. Final integration testing and validation report verification
  - Files:
    - `tests/FluentPDF.Rendering.Tests/Interop/Verification/FullSystemIntegrationTests.cs`
  - Create comprehensive integration test that runs entire validation suite end-to-end
  - Verify all validators execute, all reports generate (JSON, JUnit XML, HTML, console), all workarounds test, performance profiling completes
  - Validate report schema compliance (JSON Schema validation for JSON output)
  - Purpose: Final verification that entire system works together correctly
  - _Leverage: All previous components (validators, profilers, workaround tests, report exporters, orchestrator)_
  - _Requirements: All requirements (comprehensive final verification)_
  - _Prompt: Implement the task for spec pdfium-marshaling-verification, first run mcp__spec-workflow__spec-workflow-guide to get the workflow guide then implement the task: Role: Senior QA engineer with system integration testing expertise | Task: Create comprehensive system integration test following all requirements, running complete validation suite (all 5 validators, performance profiler, all 3 workaround tests) via MarshallingVerifier, generating all report formats (JSON, JUnit XML, HTML, console), validating JSON output against JSON Schema (if available) or structure validation, verifying report contains results from all validators with correct data (UTF-16, bitmap, annotation, threading, buffer safety, profiling metrics, workaround statuses), ensuring execution completes in < 30 seconds (performance requirement), testing with comprehensive_test.pdf | Restrictions: Test must execute full validation suite (not mocked), verify actual report content (not just file existence), validate report structure and data accuracy, ensure test completes in < 30 seconds to meet design requirement, clean up test output files after test | _Leverage: All validators, profilers, workaround tests, report exporters, MarshallingVerifier orchestrator, comprehensive_test.pdf from TestData/ | Success: Integration test executes full validation suite successfully, all 5 validators run and return results, profiling completes with metrics, all 3 workarounds tested, all 4 report formats generated (JSON, JUnit XML, HTML, console), JSON structure validated, report contains accurate data from all components, test completes in < 30 seconds, test is reliable and deterministic | After completion: Mark this task [-] as in-progress in tasks.md, then use log-implementation tool with artifacts: integration test class (FullSystemIntegrationTests), test scenario (full validation suite), components validated (5 validators, profiler, 3 workarounds, 4 report formats), execution time (< 30s), file location, then mark [x] as complete
