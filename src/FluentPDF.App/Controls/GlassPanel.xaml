<?xml version="1.0" encoding="UTF-8" ?>
<UserControl
    x:Class="FluentPDF.App.Controls.GlassPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Glass container with acrylic backdrop and elevation shadow -->
    <Border
        x:Name="GlassContainer"
        CornerRadius="{x:Bind GlassCornerRadius, Mode=OneWay}"
        Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
        BorderThickness="1"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">

        <!-- Elevation shadow effect -->
        <Border.Shadow>
            <ThemeShadow />
        </Border.Shadow>

        <!-- Content presenter -->
        <ContentPresenter
            x:Name="ContentPresenter"
            Content="{x:Bind Content, Mode=OneWay}"
            Padding="{x:Bind Padding, Mode=OneWay}"
            HorizontalContentAlignment="{x:Bind HorizontalContentAlignment, Mode=OneWay}"
            VerticalContentAlignment="{x:Bind VerticalContentAlignment, Mode=OneWay}" />
    </Border>
</UserControl>
